apiVersion: v1
kind: Service
metadata:
  name: shopsphere-backend
spec:
  selector:
    app: shopsphere-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
  